/* 
Arrow function
เป็นรูปแบบการเขียน function ให้มีความกระชับมากยิ่งขึ้น

แบบเดิม
function (ชื่อ function) (parameter){
    (สั่งภายใน function)
}

arrow function

fullname = (parameter)=>

**ในกรณีที่มีคำสั่งภายใน function แค่บรรทัดเดียวสามารถที่จะตัด {} และ return ค่าที่ส่งออกจากตัว function ออกไปได้

*/

// plusNumber = (x,y)=>console.log(x+y)
// plusNumber(10,20)

// getAge = (age) => console.log(`อายุของคุณคือ : ${age}`)
// getAge(23)



