/* 
Props (properties) ตัวแปรที่สามารถส่งเข้าไปใน components ได้ ผ่านการกำหนด attribute ส่งผลให้ component
แต่ละตัวสามารถรับค่าด้านนอกเข้าไปทำงานได้

<ชื่อ component ชื่อ properties = "ค่าที่กำหนดให้ properties"/>
*/